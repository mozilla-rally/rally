{
  "name": "rally-attention-stream",
  "version": "0.4.6",
  "scripts": {
    "build": "cp manifest.prod.json manifest.json && npm run build:glean && rollup -c 2>&1 && npm run tailwind && npm run package:webext",
    "dev": "cp manifest.dev.json manifest.json && npm run build:glean && npm run clean && npm run tailwind && npm run dev:extension",
    "dev:extension": "rollup -c --config-enable-developer-mode -w --watch.onEnd \"npm run dev:webext\"",
    "dev:extension:emulator": "rollup -c --config-enable-emulator-mode -w --watch.onEnd \"npm run dev:webext\"",
    "lint": "npm run build && eslint . && npm run lint:glean",
    "clean": "rimraf dist/*",
    "build:glean": "npm run build:glean:metrics && npm run build:glean:docs",
    "build:glean:metrics": "glean translate ./metrics.yaml ./pings.yaml -f typescript -o src/generated",
    "build:glean:docs": "glean translate ./metrics.yaml ./pings.yaml -f markdown -o docs",
    "lint:glean": "glean glinter ./metrics.yaml ./pings.yaml",
    "build:developer": "cp manifest.dev.json manifest.json && rollup -c --config-enable-developer-mode && npm run tailwind",
    "build:emulator": "cp manifest.prod.json manifest.json && rollup -c --config-enable-emulator-mode && npm run tailwind",
    "tailwind": "tailwindcss -o dist/tailwind.css 2>&1",
    "dev:emulator": "npm-run-all --parallel dev:extension:emulator dev:webext",
    "package:webext": "web-ext build",
    "package:developer": "npm run build:developer && npm run package:webext -- -n developer.zip --overwrite-dest",
    "package:emulator": "npm run build:emulator && npm run package:webext -- -n emulator.zip --overwrite-dest",
    "start": "npm run build && web-ext run",
    "test:integration": "./scripts/integration-test.sh",
    "test:integration:jest": "jest ./tests/integration/",
    "dev:webext": "web-ext run -t ${TARGET=chromium} --start-url 'https://example.com' --watch-file dist/backgrounnd.js",
    "test": "npm run package:developer 2>&1 && npm run test:integration"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.11.5",
    "@babel/preset-typescript": "^7.17.12",
    "@rollup/plugin-commonjs": "^21.0.0",
    "@rollup/plugin-node-resolve": "^13.0.6",
    "@rollup/plugin-replace": "^3.0.0",
    "@rollup/plugin-typescript": "^8.3.2",
    "@types/jest": "^27.5.0",
    "@types/selenium-webdriver": "^4.0.18",
    "chromedriver": "^103.0.0",
    "eslint": "^8.15.0",
    "eslint-plugin-import": "^2.26.0",
    "eslint-plugin-mocha": "^10.0.3",
    "eslint-plugin-node": "^11.1.0",
    "geckodriver": "^3.0.1",
    "globby": "^11.0.3",
    "http-server": "^14.1.1",
    "jest": "^28.1.0",
    "npm-run-all": "^4.1.5",
    "rollup": "^2.30.0",
    "rollup-plugin-copy": "^3.4.0",
    "selenium-webdriver": "^4.1.1",
    "ts-jest": "^28.0.2",
    "typescript": "^4.6.4",
    "web-ext": "^7.0.0"
  },
  "dependencies": {
    "@mozilla/glean": "^1.0.0",
    "@mozilla/rally-sdk": "^0.9.32",
    "@mozilla/web-science": "^0.5.2",
    "dexie": "^3.2.2",
    "pako": "^2.0.4",
    "tailwindcss": "^3.0.24",
    "webextension-polyfill": "^0.8.0"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "description": "This the Rally Attention Stream extension.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mozilla-rally/rally"
  },
  "keywords": [],
  "author": "Mozilla",
  "license": "MPL-2.0",
  "private": true,
  "bugs": {
    "url": "https://github.com/mozilla-rally/rally/issues"
  },
  "homepage": "https://github.com/mozilla-rally/rally#readme"
}
